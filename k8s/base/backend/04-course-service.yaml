---
# Service for Course Service
apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: learnivo
  labels:
    app: course-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: course-service

---
# Deployment for Course Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
  namespace: learnivo
  labels:
    app: course-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      containers:
      - name: course-service
        image: learnivo/course-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        - name: JAVA_OPTS
          value: "-Xmx512m -Xms256m"
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: "http://discovery-server:8761/eureka/"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: learnivo-secrets
              key: JWT_SECRET
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:h2:file:/data/h2/course-service;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: learnivo-secrets
              key: SPRING_DATASOURCE_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: learnivo-secrets
              key: SPRING_DATASOURCE_PASSWORD
        - name: COURSE_AI_RECOMMENDATIONS_ENABLED
          valueFrom:
            secretKeyRef:
              name: learnivo-secrets
              key: COURSE_AI_RECOMMENDATIONS_ENABLED
        - name: COURSE_AI_API_KEY
          valueFrom:
            secretKeyRef:
              name: learnivo-secrets
              key: COURSE_AI_API_KEY
              optional: true
        volumeMounts:
        - name: config
          mountPath: /workspace/config
          readOnly: true
        - name: data
          mountPath: /data
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 40
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 3
      volumes:
      - name: config
        configMap:
          name: course-service-config
      - name: data
        emptyDir: {}
