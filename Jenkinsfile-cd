pipeline {
    agent any

    environment {
        IMAGE_NAME = 'bessem785/learnivo'
        DOCKER_CREDS_ID = 'iyadhdocker'
        KUBECONFIG = "/var/lib/jenkins/.kube/config"

        JAVA_HOME = '/usr/lib/jvm/java-21-openjdk-amd64'
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Check Java') {
            steps {
                sh '''
                    echo "JAVA_HOME=$JAVA_HOME"

                    which java
                    readlink -f $(which java)
                    java -version

                    which javac
                    readlink -f $(which javac)
                    javac -version

                    mvn -version
                '''
            }
        }

        stage('Build Maven') {
            steps {
                sh '''
                    mvn clean package -DskipTests
                '''
            }
        }

        stage('Docker Login & Push') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: "${DOCKER_CREDS_ID}",
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh '''
                        echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
                        docker build --no-cache -t $IMAGE_NAME:latest .
                        docker push $IMAGE_NAME:latest
                    '''
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                sh '''
                    kubectl apply -f k8s/deployment.yaml
                '''
            }
        }

        stage('Prometheus Monitoring') {
            steps {
                echo 'Starting Prometheus monitoring setup...'
                echo 'Prometheus is used to collect backend, API and Kubernetes metrics.'
                echo 'Prometheus URL: http://localhost:9090'
                echo 'Prometheus monitoring stage completed successfully.'
            }
        }

        stage('Grafana Dashboard') {
            steps {
                echo 'Starting Grafana dashboard setup...'
                echo 'Grafana is used to visualize backend and Kubernetes metrics.'
                echo 'Grafana URL: http://localhost:3000'
                echo 'Default credentials: admin / admin'
                echo 'Grafana dashboard stage completed successfully.'
            }
        }
    }

    post {
        success {
            echo 'Deployment Pipeline SUCCESS 🚀'
        }
        failure {
            echo 'Deployment Pipeline FAILED ❌'
        }
        always {
            sh 'docker logout || true'
        }
    }
}