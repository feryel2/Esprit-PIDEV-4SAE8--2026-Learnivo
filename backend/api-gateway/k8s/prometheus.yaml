apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-api-gateway
  template:
    metadata:
      labels:
        app: prometheus-api-gateway
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus:latest
          ports:
            - containerPort: 9090
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-api-gateway
spec:
  selector:
    app: prometheus-api-gateway
  ports:
    - port: 9090
      targetPort: 9090
  type: ClusterIP
