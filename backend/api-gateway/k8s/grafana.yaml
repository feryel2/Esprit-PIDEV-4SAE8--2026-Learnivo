apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-api-gateway
  template:
    metadata:
      labels:
        app: grafana-api-gateway
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-api-gateway
spec:
  selector:
    app: grafana-api-gateway
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
