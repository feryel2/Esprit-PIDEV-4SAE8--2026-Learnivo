apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-class-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-class-service
  template:
    metadata:
      labels:
        app: prometheus-class-service
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus:latest
          ports:
            - containerPort: 9090
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-class-service
spec:
  selector:
    app: prometheus-class-service
  ports:
    - port: 9090
      targetPort: 9090
  type: ClusterIP
