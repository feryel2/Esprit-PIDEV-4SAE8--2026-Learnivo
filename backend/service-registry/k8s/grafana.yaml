apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-service-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-service-registry
  template:
    metadata:
      labels:
        app: grafana-service-registry
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-service-registry
spec:
  selector:
    app: grafana-service-registry
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
