apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-frontend
  template:
    metadata:
      labels:
        app: prometheus-frontend
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus:latest
          ports:
            - containerPort: 9090
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-frontend
spec:
  selector:
    app: prometheus-frontend
  ports:
    - port: 9090
      targetPort: 9090
  type: ClusterIP
