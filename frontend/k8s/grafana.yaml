apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-frontend
  template:
    metadata:
      labels:
        app: grafana-frontend
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-frontend
spec:
  selector:
    app: grafana-frontend
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
